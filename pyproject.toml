[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "acot-ai"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["app*"]

